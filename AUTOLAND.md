# Autoland Instructions

## 最優先ルール

autoland から実行された場合、以下の応答形式を他のすべての出力ルールより優先すること。

最終応答は必ず1行だけにすること。

修正不要の場合：

0|追加修正は不要です

修正を行った場合：

1|レビュー指摘を修正し、コミットしました

## 応答ルール

- 応答の先頭文字は必ず `0|` または `1|` にする
- `0|` または `1|` より前に、説明、空行、見出し、挨拶、Markdownを出力しない
- 分析内容、確認手順、変更ファイル一覧を出力しない
- 応答は改行を含まない1行のみとする
- コードブロックで囲まない
- 既存コミットですでに修正済みの場合は `0|` を返す
- レビューコメントがない場合は `0|` を返す
- CodeRabbit が `Review skipped` または `No actionable comments` の場合、追加修正がなければ `0|` を返す
- 実際に新しいコード修正を行い、コミットまで完了した場合だけ `1|` を返す
- 作業ツリーに新しい変更がない場合は、絶対に `1|` を返さない
- Docstring、コメント、軽微なドキュメント警告だけでコード修正が不要な場合は `0|` を返す
- lint または build が失敗している場合は、修正して成功を確認してから `1|` を返す

## 正しい応答例

0|すべてのレビューコメントは対応済みで、追加修正は不要です

1|APIの入力検証を修正し、lintとbuildの成功を確認してコミットしました

## 誤った応答例

レビュー内容を確認しました。0|追加修正は不要です

現在の状態を確認します。
0|追加修正は不要です

```text
0|追加修正は不要です
```
